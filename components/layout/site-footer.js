export function SiteFooter() {
  return (
    <footer className="border-t border-white/60 py-10">
      <div className="shell-width flex flex-col gap-4 text-sm text-[var(--ink-soft)] sm:flex-row sm:items-center sm:justify-between">
        <p>ProofFit AI is built around truth, evidence, and ATS-safe output.</p>
        <p>Premium workflow. No hidden paywall after upload. No fake claims.</p>
      </div>
    </footer>
  );
}
